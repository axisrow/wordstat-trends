# MVP #23 — вектор AutoETS, вариант Б

Это только проверка гипотезы, не продуктовая реализация.

## Предрегистрация второй итерации: дневной `sp=7`

Этот раздел записан **до получения и просмотра дневных фикстур**. Пороговые
значения ниже не будут подбираться по результату.

### Вход и парсинг

- Каждая фикстура содержит 58 реальных последовательных строк данных:
  `2026-06-23` → `2026-08-19`. Метка `2026-08-20` существует только в
  заголовке графика и не используется. Первые 56 строк (`2026-06-23` →
  `2026-08-17`) образуют обучающее окно из 8 полных недель; оставшиеся 2 дня
  (`2026-08-18` → `2026-08-19`) — контроль вне обучения.
- Дневной столбец `Дата` (старое имя `Период` также распознаётся явно)
  разбирается отдельно от месячного формата, строго как
  `datetime.strptime(value, "%d.%m.%Y")`; русский текстовый месяц здесь не
  применяется.
- Сохраняется механика реального экспорта: UTF-8 с BOM, CR-only через
  `splitlines()`, разделитель `;`, пробелы тысяч удаляются, десятичная запятая
  заменяется точкой.
- До fit проверяются границы по строкам (не по заголовку), уникальность и
  возрастание без пропущенных дней. Holdout определяется как все реальные
  строки после первых 56, а не как заранее заданная разность длин. Для
  56-дневного обучающего окна отдельно проверяется
  распределение наблюдений по дням недели `[8, 8, 8, 8, 8, 8, 8]` в порядке
  пн … вс. Недостающие дни не достраиваются. Начало окна не обязано быть
  понедельником: кратность семи уже обеспечивает равное число всех дней недели.

### Вектор и сопоставление окон

Одинаково для окон 56, 49 и 42 дня (8, 7 и 6 полных циклов) строится
`AutoETS(auto=True, sp=7, n_jobs=1, random_state=0)`. Спецификация ETS не
задаётся вручную. Для каждого fit фиксируются выбранные error/trend/seasonal.
Сезонные состояния выравниваются по дням недели **пн … вс**, а не по позиции в
окне.

Чтобы сравнивать аддитивную и мультипликативную спецификации в единой шкале,
сезонный профиль переводится в безразмерный множитель спроса `p[d]`:

- multiplicative: `p[d] = s[d] / mean(s)`;
- additive: `p[d] = 1 + (s[d] - mean(s)) / abs(level)`;
- no seasonality: `p[d] = 1` для всех дней.

Здесь `level` — финальное фильтрованное состояние соответствующего fit.

### Заранее заданный критерий устойчивости

Для каждого укороченного окна профиль сравнивается с полным. Обозначим
`A = max(p_full) - min(p_full)` и `delta = p_short - p_full`.

Здесь амплитуда `A` — именно **peak-to-trough размах нормализованного профиля**:
`max(p_full) - min(p_full)`. Порог `0,05` означает размах в 5 процентных
пунктов относительно среднего множителя `1`; это не отношение `max/min` и не
СКО относительно уровня.

Неплоский профиль (`A >= 0,05`) считается устойчивым, только если одновременно:

1. корреляция Pearson по 7 коэффициентам `corr >= 0,90`;
2. относительный RMSE коэффициентов
   `sqrt(mean(delta^2)) / sqrt(mean((p_full - 1)^2)) <= 0,25`;
3. максимальный сдвиг относительно амплитуды
   `max(abs(delta)) / A <= 0,35`.

Все три условия должны пройти **и для 49, и для 42 дней у каждой фразы**.
Смена наличия сезонной компоненты между окнами автоматически означает провал
устойчивости, даже если численные метрики случайно проходят.

Пороги оставлены без изменений: они заданы в относительных единицах профиля и
не обосновывались числом циклов. Самое короткое окно содержит шесть полных
циклов, то есть втрое больше минимальных двух для сезонной инициализации
statsmodels.

Если полный профиль плоский (`A < 0,05`), корреляция и деление на малую
амплитуду не интерпретируются: он заранее классифицируется как неосмысленный
для гипотезы недельного вектора. Для прозрачности численные значения всё равно
выводятся как `N/A`, а не заменяются нулём.

### Отдельный вопрос: оправдана ли сезонность

Второй backend — автоматический ETS из statsforecast с `season_length=7`; его
спецификация также не фиксируется руками. Эта кросс-проверка отвечает не на
вопрос устойчивости вектора, а на самостоятельный вопрос: оправдана ли
сезонная компонента вообще.

Для каждого окна и backend будут отдельно записаны:

- автоматически выбранная спецификация, включая наличие сезонности;
- AIC/AICc выбранной модели и, когда backend предоставляет кандидатов, значения
  лучшей сезонной и лучшей несезонной модели вместе с разностью критерия;
- Pearson и `max(abs(p_statsforecast - p_sktime)) / A_sktime` между профилями,
  когда оба backend выбрали сезонность и сравнение определено.

Согласие backend **не является критерием прохождения**. Если один backend
отверг сезонность, будет указано, какой именно, по какому информационному
критерию и с какими числами. Такое расхождение — самостоятельный измеренный
результат, а не успех или провал эксперимента и не повод вручную выбирать
спецификацию.

Провалом гипотезы считается неустойчивость по трём заранее заданным метрикам
выше либо плоский полный профиль (`A < 0,05`), в котором нечего различать.

Перед вычислительным прогоном потоки ограничиваются:

```bash
export OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 NUMEXPR_NUM_THREADS=2
```

## Вторая итерация (дневные, `sp=7`) — измеренный результат

Дата прогона: 2026-08-21. Команда воспроизведения:

```bash
export OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 NUMEXPR_NUM_THREADS=2
python scripts/experiment_vector_b.py > result.json
```

### Что измерено

#### Вход и границы

Все три файла сохранили BOM и CR-only (`59 CR`, `0 LF`), содержат 58 строк
данных `2026-06-23` → `2026-08-19`. Первые 56 строк заканчиваются
`2026-08-17`; число наблюдений пн … вс равно `[8, 8, 8, 8, 8, 8, 8]`.
Последние две строки (`18–19.08`) не участвовали в fit.

| Файл | SHA-256 |
|---|---|
| `dynamics_daily_high_freq_mvp.csv` | `741320f6c96d721677f9134c8eb724fd174ff33a4a2f270ab4bedea061bd659a` |
| `dynamics_daily_mid_freq_mvp.csv` | `16947481b148e46f76cbd1295240233cd859847d72129d716fd292bbfadfac90` |
| `dynamics_daily_seasonal_mvp.csv` | `070877be1a5f2590bc55292a35f413ff63a3fa48f708ede974276277c2a702d1` |

#### Автоматически выбранные модели

sktime оставлен с его default-критерием AIC; statsforecast — с default AICc.
В таблице показаны `AIC / AICc`. `N` означает отсутствие компоненты, `Ad` —
демпфированный аддитивный тренд.

| Фраза | Дней | sktime AutoETS | AIC / AICc | statsforecast AutoETS | AIC / AICc |
|---|---:|---|---:|---|---:|
| купить телефон | 56 | A,N,N | 1043,101 / 1043,563 | A,N,A | 1094,738 / 1099,627 |
| купить телефон | 49 | A,N,N | 916,404 / 916,937 | A,N,A | 955,958 / 961,748 |
| купить телефон | 42 | A,N,N | 785,640 / 786,271 | A,N,A | 816,276 / 823,372 |
| курсы английского | 56 | M,A,M | 671,948 / 682,192 | M,N,M | 739,787 / 744,676 |
| курсы английского | 49 | A,A,A | 582,052 / 594,405 | A,Ad,A | 633,612 / 644,012 |
| курсы английского | 42 | A,A,A | 501,483 / 517,039 | M,N,M | 545,048 / 552,144 |
| новогодние подарки | 56 | M,A,M | 724,418 / 733,085 | M,N,M | 790,093 / 794,981 |
| новогодние подарки | 49 | M,A,M | 631,514 / 641,914 | M,N,M | 681,719 / 687,508 |
| новогодние подарки | 42 | A,N,A | 537,360 / 546,160 | A,N,A | 573,149 / 580,245 |

#### Полные недельные профили

Порядок коэффициентов: **пн, вт, ср, чт, пт, сб, вс**. Это безразмерные
множители со средним `1`, поэтому профили разных спецификаций сопоставимы.

| Фраза | Backend | Уровень | Тренд | Профиль пн … вс | Амплитуда |
|---|---|---:|---:|---|---:|
| купить телефон | sktime | 49 809,705 | 0,000 | 1,0000; 1,0000; 1,0000; 1,0000; 1,0000; 1,0000; 1,0000 | 0,0000 |
| купить телефон | statsforecast | 48 380,616 | 0,000 | 1,0295; 1,0025; 1,0099; 1,0342; 0,9923; 0,9564; 0,9751 | 0,0778 |
| курсы английского | sktime | 1 613,682 | -1,180 | 1,0962; 1,0573; 1,1067; 1,0840; 0,9623; 0,7957; 0,8978 | 0,3110 |
| курсы английского | statsforecast | 1 495,074 | 0,000 | 1,0945; 1,0622; 1,1091; 1,0821; 0,9591; 0,7958; 0,8972 | 0,3133 |
| новогодние подарки | sktime | 1 662,923 | 20,728 | 1,1689; 1,1407; 1,1528; 1,1476; 0,9772; 0,6278; 0,7849 | 0,5411 |
| новогодние подарки | statsforecast | 1 433,959 | 0,000 | 1,1762; 1,1413; 1,1512; 1,1448; 0,9756; 0,6281; 0,7828 | 0,5482 |

#### Главная проверка устойчивости

Пороги не менялись после просмотра данных: корреляция `>= 0,90`, relative
RMSE `<= 0,25`, max shift / amplitude `<= 0,35`, amplitude `>= 0,05`.

| Фраза | Backend | Рефит | Corr | Relative RMSE | Max shift / amplitude | Gate |
|---|---|---:|---:|---:|---:|---|
| купить телефон | sktime | 49 | N/A | N/A | N/A | **FAIL: amplitude=0** |
| купить телефон | sktime | 42 | N/A | N/A | N/A | **FAIL: amplitude=0** |
| купить телефон | statsforecast | 49 | 0,9897 | 0,1447 | 0,0811 | PASS |
| купить телефон | statsforecast | 42 | 0,9593 | **0,2905** | 0,1532 | **FAIL: RMSE** |
| курсы английского | sktime | 49 | 0,9961 | 0,1280 | 0,0884 | PASS |
| курсы английского | sktime | 42 | 0,9973 | 0,0848 | 0,0499 | PASS |
| курсы английского | statsforecast | 49 | 0,9942 | 0,1201 | 0,0847 | PASS |
| курсы английского | statsforecast | 42 | 0,9981 | 0,0662 | 0,0331 | PASS |
| новогодние подарки | sktime | 49 | 0,9988 | 0,0512 | 0,0294 | PASS |
| новогодние подарки | sktime | 42 | 0,9939 | 0,2408 | 0,1250 | PASS |
| новогодние подарки | statsforecast | 49 | 0,9988 | 0,0504 | 0,0281 | PASS |
| новогодние подарки | statsforecast | 42 | 0,9934 | 0,1651 | 0,1008 | PASS |

Итог gate по фразам: «курсы английского» — PASS, «новогодние подарки» —
PASS, «купить телефон» — FAIL. Общий gate «все фразы и оба backend» — FAIL.

#### Оправданность сезонности и кросс-проверка

Диагностический перебор statsforecast не меняет основной automatic fit: он
отдельно находит лучшую сезонную и несезонную модель. В таблице
`delta AICc = AICc(seasonal) - AICc(nonseasonal)`; отрицательное значение в
пользу сезонности.

| Фраза | Дней | Delta AICc seasonal−nonseasonal | Решение sktime / statsforecast |
|---|---:|---:|---|
| купить телефон | 56 | -9,985 | nonseasonal / seasonal |
| купить телефон | 49 | -6,410 | nonseasonal / seasonal |
| купить телефон | 42 | -0,259 | nonseasonal / seasonal |
| курсы английского | 56 | -71,464 | seasonal / seasonal |
| курсы английского | 49 | -58,112 | seasonal / seasonal |
| курсы английского | 42 | -47,039 | seasonal / seasonal |
| новогодние подарки | 56 | -52,081 | seasonal / seasonal |
| новогодние подарки | 49 | -42,234 | seasonal / seasonal |
| новогодние подарки | 42 | -35,938 | seasonal / seasonal |

На полном окне профили двух backend для «курсов английского» имеют корреляцию
`0,99973`, max shift/amplitude `0,01593`; для «новогодних подарков» —
`0,99988` и `0,01359`. Для «купить телефон» сравнение профилей не определено:
sktime выбрал несезонную модель, statsforecast — сезонную. Это расхождение
зафиксировано как результат, а не превращено в gate.

#### Осмысленность и двухдневный контроль

Проверка осмысленности выполнялась только для фраз, прошедших stability gate.

| Фраза | Backend | Среднее будни | Среднее выходные | Разница |
|---|---|---:|---:|---:|
| курсы английского | sktime | 1,0613 | 0,8467 | 0,2146 |
| курсы английского | statsforecast | 1,0614 | 0,8465 | 0,2149 |
| новогодние подарки | sktime | 1,1175 | 0,7064 | 0,4111 |
| новогодние подарки | statsforecast | 1,1178 | 0,7055 | 0,4124 |

Holdout 18–19 августа носит описательный характер, порог для него не
предрегистрировался.

| Фраза | Backend | MAE | sMAPE |
|---|---|---:|---:|
| купить телефон | sktime | 1 069,205 | 2,170% |
| купить телефон | statsforecast | 59,534 | 0,122% |
| курсы английского | sktime | 85,464 | 4,943% |
| курсы английского | statsforecast | 112,356 | 6,582% |
| новогодние подарки | sktime | 279,946 | 15,137% |
| новогодние подарки | statsforecast | 339,276 | 18,653% |

### Вывод

Гипотеза **частично подтверждена, но общий MVP-gate не пройден**. На двух из
трёх реальных рядов недельные векторы устойчивы на 56/49/42 днях сразу в двух
backend, backend практически совпадают по форме, а профиль содержательно
разделяет будни и выходные. Это сильнее отрицательного результата месячного
`sp=12`: шесть-восемь циклов действительно позволяют рефит.

Однако единым представлением произвольного спроса такой вектор пока считать
нельзя. Для «купить телефон» sktime во всех окнах отверг сезонность и вернул
плоский профиль; statsforecast выбрал слабую сезонность, но на 42 днях её
relative RMSE `0,2905` превысил заранее заданный предел `0,25`, а преимущество
сезонной модели сократилось до `0,259 AICc`. То есть именно на высокочастотной
фразе вывод зависит от backend и длины окна.

Практический результат MVP: недельный вектор пригоден как **условный признак
только после проверки наличия и устойчивости сезонности**, а не как
гарантированный фиксированный слой для каждой фразы. До продуктовой
архитектуры нужны более длинные окна/повторные срезы и правило обработки фраз,
для которых automatic ETS отвергает сезонность.

### Чего проверить не удалось

- Два holdout-дня недостаточны для вывода о прогнозном качестве.
- Три фразы недостаточны для оценки доли запросов с устойчивой недельной
  сезонностью или качества будущей кластеризации.
- Результат относится к одному 56-дневному календарному срезу; устойчивость
  между разными датами сбора не измерена.

## Первая итерация (месячные, `sp=12`) — отрицательный результат

Дата прогона: 2026-08-21. Исходная воспроизводимая реализация этой итерации
зафиксирована в commit `d5bcead`; текущий скрипт переведён на дневную вторую
итерацию.

### Протокол

- Источник: три реальные CSV-фикстуры по 24 месячных наблюдения, август 2024 —
  июль 2026.
- Чтение: UTF-8 с BOM, CR-only разбивается через `splitlines()`, `;`, русский
  текстовый месяц, пробелы в числе запросов удаляются.
- Модель: `sktime.forecasting.ets.AutoETS(auto=True, sp=12, n_jobs=1,
  random_state=0)`; Python 3.12.10, sktime 1.1.0, statsmodels 0.14.6.
- Вектор фиксированной длины: финальный фильтрованный уровень, финальный тренд
  (`0`, если AutoETS выбрал модель без тренда), затем последние фильтрованные
  сезонные состояния для января … декабря. Состояния привязаны к календарному
  месяцу, а не к позиции в окне.
- Для устойчивости тот же расчёт должен быть выполнен после удаления последних
  3 и 6 месяцев. Заранее заданный предел заметного дрейфа — 20% для уровня,
  тренда и относительной L2-нормы сезонного профиля.
- Стоп-условие: если укороченные векторы не получены или выходят за пределы,
  проверка осмысленности профилей не выполняется.

### Что измерено

### Загрузка данных

Каждая фикстура разобрана в 24 последовательных месячных значения без пропусков:
начало `2024-08`, конец `2026-07`. Контрольная точка сезонной фикстуры:
декабрь 2024 = `1 234 632` запросов.

### Векторы полного окна

Порядок сезонных коэффициентов во всех строках: **янв, фев, мар, апр, май,
июн, июл, авг, сен, окт, ноя, дек**.

| Профиль | Выбранная ETS | Уровень | Тренд | AICc | Сезонные коэффициенты янв … дек |
|---|---:|---:|---:|---:|---|
| купить телефон | M,N,M | 1 229 347,969 | 0,000 | 692,206 | 0,8288; 0,7244; 0,7533; 0,6944; 0,6825; 0,8495; 0,9993; 1,0000; 0,9056; 0,9087; 0,9049; 0,9541 |
| курсы английского языка | M,N,M | 44 198,919 | 0,000 | 579,831 | 0,9755; 0,9039; 0,8847; 0,8474; 0,9695; 0,9738; 0,9266; 1,0000; 1,5240; 1,1542; 1,1314; 0,9448 |
| новогодние подарки | M,A,M | 135 779,868 | -3 118,438 | 649,150 | 1,0459; 0,3044; 0,2083; 0,1689; 0,1589; 0,1368; 0,2137; 0,2799; 0,6301; 1,3916; 2,8275; 7,2577 |

`M,N,M` означает multiplicative error, no trend, multiplicative seasonality;
`M,A,M` — multiplicative error, additive trend, multiplicative seasonality.

### Главная проверка устойчивости

Результат одинаков для всех трёх профилей:

| Окно | Наблюдений | Конец окна | AutoETS `sp=12` | Численное сравнение с полным окном |
|---|---:|---:|---|---|
| полное | 24 | 2026-07 | вектор получен | база сравнения |
| минус 3 месяца | 21 | 2026-04 | ошибка | невозможно |
| минус 6 месяцев | 18 | 2026-01 | ошибка | невозможно |

Точный результат всех шести укороченных запусков:

```text
ValueError: Cannot compute initial seasonals using heuristic method with less
than two full seasonal cycles in the data.
```

Полученные численные пары для метрик дрейфа: **0 из 6**. Поэтому значения
`level_relative`, `trend_relative` и `seasonal_relative_l2` отсутствуют, а не
равны нулю.

### Вывод

**MVP на имеющихся фикстурах не подтверждает состоятельность вектора.** 24 точки
— ровно два полных цикла `sp=12`, то есть минимальный объём, на котором AutoETS
вообще получил сезонную инициализацию. После обязательного усечения остаётся 21
или 18 точек, и модель не строится. Следовательно, устойчивость вектора численно
не проверена; использовать его как представление спроса и переносить идею в
продукт по этому результату нельзя.

Это отрицательный/неопределённый результат по стоп-условию, а не свидетельство
устойчивости или неустойчивости коэффициентов. Для честного повторения нужен
более длинный месячный ряд из #6: как минимум 30 точек, чтобы после удаления
6 месяцев осталось два цикла; практически лучше несколько полных циклов сверх
этого минимума.

### Чего проверить не удалось

- Численный дрейф векторов при усечении: укороченные векторы не существуют.
- Осмысленность: сравнение декабрьского пика «новогодних подарков» с более
  плоским профилем «купить телефон» **не выполнялось**, потому что главная
  проверка не прошла.
- `sp=7` и дневные данные не рассматривались.
