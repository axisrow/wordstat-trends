"""NLP над фразами запросов (Фаза 4, issue #13).

Только тексты фраз — временные ряды здесь не участвуют. Тяжёлые зависимости
(эмбеддинги, torch) живут в extra `nlp` и импортируются лениво; лемматизация
(pymorphy3) входит в основной набор.
"""
