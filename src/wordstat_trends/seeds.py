"""Seed-политика проекта: единая точка правды о стохастике (issue #95).

Аудит мест стохастики по состоянию ветки main (все экспериментальные
скрипты и ``wordstat_trends.forecasting``):

Есть стохастика — фиксируется seed'ом отсюда:
- ``wordstat_trends.forecasting.models.RANDOM_STATE`` — init-розыгрыш
  AutoETS(auto=True); равен :data:`DEFAULT_SEED`.
- ``scripts/issue13_tfidf_vs_berta.py`` — k-means по нескольким seed'ам
  (:data:`KMEANS_SEEDS`) ради метрики стабильности ARI к инициализации.

Есть стохастика, но seed — ИСТОРИЧЕСКИЙ (артефакт уже записан с ним,
менять значение = ломать побайтовую воспроизводимость закрытого прогона;
менять можно только место определения, не число):
- ``scripts/issue32_seasonal_structure.py`` — ``SEED = 32`` для
  симуляции N_REPS розыгрышей (закрытый прогон #32);
- ``scripts/issue34_window_threshold.py`` — ``np.random.seed(0)``:
  калибровочный рецепт побран перебором под таблицу issue #34.

Стохастики НЕТ (зафиксировано здесь, чтобы не искать при каждом ревью):
- ``scripts/experiment_vector_d.py`` и ``scripts/issue23_combined_vector.py``
  — ETS-фиты детерминированы фиксированной сеткой спецификаций, seed
  неприменим (детерминизм вещественной арифметики — ограничение
  BLAS-потоков до импорта numpy, см. докстринги скриптов);
- ``scripts/backtest_forecasters.py`` — детерминированные фолды
  ``ExpandingWindowSplitter`` и фиксированный ``RANDOM_STATE`` моделей;
- ThetaForecaster, сезонный наивный бейзлайн, TF-IDF/лемматизация,
  детерминированный фейк-эмбеддинг issue #13 (md5 от лемм) — без
  случайности по построению.
"""

#: Seed по умолчанию для одиночных мест стохастики (random_state моделей,
#: инициализация RNG). Значение 42 — историческое: с ним записан
#: RANDOM_STATE из ``forecasting/models.py`` (фаза 2.2, #74).
DEFAULT_SEED = 42

#: Сетка seed'ов k-means в issue #13: итоговое разбиение — seed с
#: максимальным силуэтом, ARI между всеми парами — метрика стабильности к
#: инициализации. Порядок значим (детерминированный tie-break «первый
#: максимум в порядке seeds»).
KMEANS_SEEDS = (0, 1, 2, 42)

__all__ = ["DEFAULT_SEED", "KMEANS_SEEDS"]
